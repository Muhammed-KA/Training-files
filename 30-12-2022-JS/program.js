// foreach instead of for loop.
const languages = ['c', 'c++', 'java', 'python'];
languages.forEach(lang => {
    console.log(lang);
});

// languages.forEach(lang => console.log(lang));    // single line by removing curly bracket.


const lang2 = ['c', 'c++', 'java', 'python'];
lang2.forEach